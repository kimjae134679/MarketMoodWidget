name: Debug Toss Ticker

on:
  workflow_dispatch:
  push:
    branches: [ main ]
    paths:
      - '.github/workflows/debug-toss.yml'

jobs:
  debug:
    runs-on: ubuntu-latest
    steps:
      - name: Probe Toss index APIs
        shell: bash
        run: |
          UA='Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/136.0.0.0 Safari/537.36'
          BASE='https://www.tossinvest.com'
          for u in \
            "$BASE/api/v4/dashboard/wts/overview/indicator" \
            "$BASE/api/v1/dashboard/wts/overview/indicator/KOSPI" \
            "$BASE/api/v1/dashboard/wts/overview/indicator/KOSDAQ" \
            "$BASE/api/v1/dashboard/wts/overview/indicator/NASDAQ" \
            "$BASE/api/v1/index-prices/KGG01P" \
            "$BASE/api/v1/index-prices/COMP.NAI" \
            "$BASE/api/v2/index-infos/KGG01P" \
            "$BASE/api/v2/index-infos/COMP.NAI" \
            "$BASE/api/v2/dashboard/wts/overview/tics/KGG01P" \
            "$BASE/api/v2/dashboard/wts/overview/tics/COMP.NAI"; do
            echo "===== $u ====="
            curl -sSL --compressed --connect-timeout 3 --max-time 6 -A "$UA" -H 'Accept: application/json' -H 'Referer: https://www.tossinvest.com/' -w '\nHTTP=%{http_code}\n' "$u" | head -c 6000 || true
            echo
          done
